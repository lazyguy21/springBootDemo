<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.yyf.springBootDemo.dao.DataTypeMapper">
    <insert id="insertOne">
      INSERT INTO test.data_type (age, money, dm, dc, t1, t2)
         VALUES (123, 111.112, null, null, null, null);
    </insert>

    <select id="listDataType" resultType="java.util.Map">
        select * from data_type
    </select>
</mapper>